node_modules
.next
.git
.github
.vercel
.godot
client
web/node_modules
web/.next
web/public/game
server/node_modules
assets-import
docs
*.log
*.md
.env
.env.*
# Rust build artifacts — the sim-builder Docker stage rebuilds them from
# source, so don't ship our local (possibly stale, possibly Windows-only)
# target/ into the build context.
sim/target
# Keep these — we want the server source + workspace files + sim source
!server
!sim
!package.json
!pnpm-workspace.yaml
!pnpm-lock.yaml
